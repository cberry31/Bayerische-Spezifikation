There are two ways to run our application 

1. Just click on the HomePage.html file and it will boot up

2. If you want to run it from the server 
    1. Find the repo in terminal 
    2. Run the command npm install
    3. Move into the app directory 
    4. Run the command node server.js 
    5. Go to localhost::8080